def saveDataFrameTocsv(self, directory_name, file_name, data_frame,**kwargs):
""" :param directory_name: source directory name :param file_name: file name in which datafarme need to be save :param data_frame: pandas dataframe object :return: True if dataframe saved into azure blob """
try: self.dir_list = [container_name.name for container_name in self.blob_service_client.list_containers()] allowed_keys=['index','header','mode'] self.__dict__.update((k, v) for k, v in kwargs.items() if k in allowed_keys) directory_name=directory_name.lower() if file_name.split(".")[-1] != "csv": file_name = file_name + ".csv" if directory_name not in self.dir_list: self.createDirectory(directory_name) if file_name in self.getAllFileNameFromDirectory(directory_name) and 'mode' in self.__dict__.keys(): if self.mode == 'a+':